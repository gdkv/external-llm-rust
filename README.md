# LangChain и LangGraph


### 1. Подключение к внешней LLM по API

Читаем конфиг, делаем подключение к внешней GPT, передаем вопрос введенный в readLine и читаем ответ

### 2. Если закрыт доступ в интернет то подключаемся к локальной LLM

Читаем конфиг, делаем подключение к внешней GPT, передаем вопрос введенный в readLine и читаем ответ
